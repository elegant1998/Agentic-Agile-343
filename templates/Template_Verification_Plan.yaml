version: "1.0"
task_id: "T-XXX"
status: "DRAFT" # DRAFT must be explicitly changed to AUTHORIZED by the intent owner.
obligations:
  - id: "VP-001"
    traces_to: "AC-001" # AC-*, PRESERVE-* or CONSTRAINT-*
    risk: "medium" # low | medium | high | safety_critical
    required_layers: ["unit", "interface"]
    evidence_sources:
      - path: "governance/evidence/example.json" # project-relative file only
        layer: "unit"
        producer: "implementation-team"
        verifier: "test-runner"
        independence: "SELF" # SELF | CROSS_CHECK | INDEPENDENT | HUMAN_ACCEPTANCE
        kind: "test" # test | scan | observation | acceptance | llm_judge
    producer: "implementation-team"
    verifier: "verification-owner"
    independence_required: "CROSS_CHECK"
    freshness_hours: 24
    verdict: "UNKNOWN" # PASS | FAIL | UNKNOWN | CONDITIONAL | ESCALATED
    # CONDITIONAL additionally requires: condition, owner, deadline, reverify
