version: "1.0"
task_id: T-XXX
status: DRAFT  # DRAFT | AUTHORIZED | CAPTURED
targets: []
preserve:
  # - id: PB-001
  #   behavior: "需要保持的既有可观察行为"
  #   source: "IO confirmed"  # 或 existing test
observations:
  # - id: OBS-001
  #   argv: [python3, -m, unittest, tests.test_example]
  #   cwd: .
  #   timeout_seconds: 120
  #   expected_exit_code: 0
normalization:
  redact_patterns: []
  ignore_line_patterns: []
unknown: []
