# AI 编码规范：[项目名称]

> **定位**: 本文档是 L2 全局约束的一部分。OA 在项目初始化时填充，
> 由 `crop_context.py` 自动注入 §1+§2 到每次 AS 任务上下文中。
>
> **核心原则**: 不重复预训练知识（如"变量命名要有意义"），
> 只写**本项目特有的架构决策**和**踩坑教训**。
>
> **填写指引**: 见每节下方的 `💡` 提示。不确定的先留空，项目推进中逐步补充。

**版本**: 1.0
**适用技术栈**: [待 OA 填写，如: Python 3.11 / FastAPI / PostgreSQL]
**基于基线**: [待 OA 填写，如: PEP 8 / Google Python Style Guide]（无需重复注入基线内容）

---

## 1. 硬性红线（≤5 条）

> ⚠️ 每条红线必须包含：规则 + 原因 + 违反后果。
> AI 理解了"为什么"后，能在类似场景举一反三。
>
> 💡 **填写指引**: 问自己"这个项目里，AI 最容易犯什么错误？"
> 把前 5 个答案写进来。每条不超过一行。
>
> 📋 **常用红线参考**（按项目类型选用，不合适的删除）:
> - 确定性逻辑（金额计算/状态机/正则匹配/路由分发）用代码实现，不要让 AI 推理 — AI 在数学计算和状态转换上不稳定
> - 金额字段一律 int（分为单位），禁止 float — Python 浮点精度问题
> - API 响应统一格式 — 前端拦截器依赖
> - 禁止在 router 层直接操作数据库 — 必须经 service 层，否则测试无法 mock

| # | 规则 | 原因 | 违反后果 |
|---|------|------|----------|
| 1 | [待 OA 填写] | [待 OA 填写] | [待 OA 填写] |
| 2 | [待 OA 填写] | [待 OA 填写] | [待 OA 填写] |
| 3 | [待 OA 填写] | [待 OA 填写] | [待 OA 填写] |
| 4 | [待 OA 填写] | [待 OA 填写] | [待 OA 填写] |
| 5 | [待 OA 填写] | [待 OA 填写] | [待 OA 填写] |

---

## 2. 分层约束

> 按代码层级定义"能做什么 / 禁止做什么"。
> AI 生成代码时按文件操作，分层约束匹配其工作模式。
>
> 💡 **填写指引**: 只写本项目实际存在的层级。
> - Web 项目: Router / Service / Model / Test
> - CLI 项目: CLI入口 / 核心逻辑 / 工具函数 / Test
> - Lib 项目: 公开 API / 内部实现 / Test
> 不需要的层级直接删除。

### 2.1 [层名，如 Router 层]（`[文件路径]`）

| ✅ 能做什么 | ❌ 禁止做什么 |
|------------|--------------|
| [待 OA 填写] | [待 OA 填写] |
| [待 OA 填写] | [待 OA 填写] |

### 2.2 [层名，如 Service 层]（`[文件路径]`）

| ✅ 能做什么 | ❌ 禁止做什么 |
|------------|--------------|
| [待 OA 填写] | [待 OA 填写] |
| [待 OA 填写] | [待 OA 填写] |

### 2.3 [层名]（`[文件路径]`）

| ✅ 能做什么 | ❌ 禁止做什么 |
|------------|--------------|
| [待 OA 填写] | [待 OA 填写] |

### 2.4 [层名]（`[文件路径]`）

| ✅ 能做什么 | ❌ 禁止做什么 |
|------------|--------------|
| [待 OA 填写] | [待 OA 填写] |

---

## 3. 补充禁止项（聚合所有红线）

> 以下条目由 OA 根据项目历史踩坑教训持续追加。
> 来源：图谱 §5 历史上下文中的教训 + 反思日志。
>
> 💡 **填写指引**: 此处是 §1 的"候补清单"。
> 当某个禁止项在多个任务中反复出现问题时，升级到 §1。

1. [待追加 — 项目推进中逐步填写]
2. [待追加]
3. [待追加]

---

## 4. AI 自检清单

> AI 在生成代码后、提交前逐条自问。
> 可通过 `self_consistency_check.py` 自动验证标记为 `[AUTO]` 的条目。
>
> 💡 **填写指引**: 把 §1 的每条红线转换成疑问句，能自动检查的加 `[AUTO]`。
> 再加 2-3 条通用检查项（密钥泄露、依赖声明等）。

- [ ] [待 OA 填写]
- [ ] [待 OA 填写]
- [ ] [待 OA 填写]
- [ ] [待追加]

---

## 5. 历史踩坑记录（自动反哺）

> 来源：`reflect.py --feedback-to-graph` → 图谱 §5 历史上下文。
> OA 定期将教训提炼为编码规范条目，追加到 §1 或 §3。
>
> 💡 **填写指引**: 不需要手动填。每个 SCOPE-V 周期结束后，
> 检查图谱 §5 是否有新的教训值得固化为规范。

| 日期 | 任务 | 踩坑 | 提炼的规范 |
|------|------|------|-----------|
| [待填充] | [待填充] | [待填充] | [待填充] |

---

> **维护规则**: OA 在每个 SCOPE-V 周期结束后检查图谱 §5 的新教训，
> 判断是否需要更新本文档。IO 签署后生效，注入到后续所有 AS 任务。
