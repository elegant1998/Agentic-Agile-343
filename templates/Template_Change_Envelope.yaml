# 保存为 governance/change/Change_Envelope_<TASK>.yaml；每份围栏只绑定一个任务。
version: "1.1"
task_id: T-XXX
status: PENDING  # PENDING | AUTHORIZED；只有 IO 确认后才可改为 AUTHORIZED
source_recon: governance/recon/Recon_Baseline.md
allowed:
  paths: []       # 精确文件，或以 / 结尾的目录前缀；禁止 glob、绝对路径、.. 和项目根
  capabilities: []
protected:
  paths: []
  behaviors: []
unknown: []
expansion_rule: "Change Envelope 扩大前必须更新契约并由 IO 重新签署"
