# Local self-governance artifacts are evidence for this checkout, not skill distribution files.
/governance/
/tests/

# Python runtime caches.
__pycache__/
*.py[cod]

# macOS metadata.
.DS_Store
assets/dashboard.html
assets/dashboard.html

# Team-mode local map fallback; CI-authoritative snapshots use the parent directory.
governance/recon/.local/
docs/关于codebase-memory-mcp和iwe双地图的构建与git 管理规则.md
